<%= form_for(bizowners_review) do |f| %>
  <% if bizowners_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bizowners_review.errors.count, "error") %> prohibited this bizowners_review from being saved:</h2>

      <ul>
      <% bizowners_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :bizowner_review_star %>
    <%= f.number_field :bizowner_review_star %>
  </div>

  <div class="field">
    <%= f.label :business_review_description %>
    <%= f.text_area :business_review_description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
